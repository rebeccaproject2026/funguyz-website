import React from 'react';

const categories = [
  'magic-mushrooms',
  'edibles',
  'capsules',
  'microdose'
];


export default function CategorySlugLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
